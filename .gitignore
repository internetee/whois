whois.pid
whois.output
whois.log

*.rbc
capybara-*.html
.rspec
/db/*.sqlite3
/public/system
/public/assets
/coverage/
/spec/tmp
**.orig
config/initializers/secret_token.rb
config/secrets.yml
#config/database.yml
config/deploy.rb
/export

## Environment normalisation:
/.bundle
/vendor/bundle

# Do not share secrets via git:
.env

# these should all be checked in to normalise the environment:
# Gemfile.lock, .ruby-version, .ruby-gemset

# unless supporting rvm < 1.11.0 or doing something fancy, ignore this:
.rvmrc
.DS_Store